{
  "name": "harnet",
  "version": "0.1.0",
  "description": "Agent orchestration over live TUI sessions (tmux) + git worktree",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "tsc --noEmit -p jsconfig.json",
    "start": "node src/panel/server.js"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/ws": "^8.18.1",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
